apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simpson-netpols
  namespace: openshift-gitops
spec:
  destination:
    namespace: simpson
    server: https://kubernetes.default.svc
  project: default
  source:
    path: netpol/overlays/simpson-netpols
    repoURL: https://github.com/RedHat-EMEA-SSA-Team/ns-apps
    targetRevision: netpol
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
    - CreateNamespace=true
