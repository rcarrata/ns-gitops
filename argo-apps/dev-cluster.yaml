apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-cluster
spec:
  destination:
    namespace: openshift-gitops
    server: https://kubernetes.default.svc
  project: dev-envs
  source:
    path: components/dev-cluster
    repoURL: https://github.com/RedHat-EMEA-SSA-Team/ns-gitops
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
